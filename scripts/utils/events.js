const repoEvents = () => {
  return;
};

const packageEvents = () => {
  return;
};

const projectsEvents = () => {
  return;
};

const overviewEvents = () => {
    return;
};

export {
  repoEvents,
  packageEvents,
  projectsEvents,
  overviewEvents
}